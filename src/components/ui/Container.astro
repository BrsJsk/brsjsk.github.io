---
interface Props {
	wide?: boolean;
	class?: string;
}

const { wide = false, class: className } = Astro.props;
---

<div class:list={[wide ? 'u-container' : 'u-narrow', className]}>
	<slot />
</div>
