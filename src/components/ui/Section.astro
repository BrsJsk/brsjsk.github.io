---
interface Props {
	id?: string;
	class?: string;
	tight?: boolean;
}

const { id, class: className, tight = false } = Astro.props;
---

<section id={id} class:list={[tight ? undefined : 'u-section', className]}>
	<slot />
</section>
